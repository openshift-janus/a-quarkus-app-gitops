---
apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: {{ .Values.app.name }}-pipeline
  labels:
    type: pipeline
spec:
  params:
    - name: source-repo
      description: source repo that contains the application code
      type: string
      default: {{ .Values.git.repo }}
    - name: source-branch
      description: source branch to build from
      default: {{ .Values.git.branch }}
      type: string
    - name: output-image
      description: reference of the image that will get created
      default: {{ include "image.url" . }}
      type: string
  tasks:
    - name: git-clone
      params:
        - name: url
          value: $(params.source-repo)
        - name: revision
          value: $(params.source-branch)
      taskRef:
        kind: ClusterTask
        name: git-clone
      workspaces:
        - name: output
          workspace: source-folder
    - name: package
      runAfter:
        - git-clone
      taskRef:
        kind: ClusterTask
        name: maven
      workspaces:
        - name: source
          workspace: source-folder
        - name: maven-settings
          workspace: maven-settings
    - name: build-and-push
      params:
        - name: IMAGE
          value: $(params.output-image)
      runAfter:
        - package
      taskRef:
        kind: Task
        name: kaniko
      workspaces:
        - name: source
          workspace: source-folder
    - name: rox-image-check
      runAfter:
        - build-and-push
      taskRef:
        kind: ClusterTask
        name: rox-image-check
      params:
        - name: rox_central_endpoint
          value: roxsecrets
        - name: rox_api_token
          value: roxsecrets
        - name: image
          value: >-
            image-registry.openshift-image-registry.svc:5000/a-quarkus-app-dev/a-quarkus-app
        - name: image_digest
          value: latest
  workspaces:
    - name: source-folder
    - name: maven-settings
