apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  creationTimestamp: null
  name: app-a-quarkus-app-binding
spec:
  params:
  - name: MAVEN_MIRROR_URL
    value: '{{ $.Values.app.nexusHostUrl }}/repository/maven-public/'

  - name: SONARQUBE_HOST_URL
    value: "{{ $.Values.app.SONARQUBE_HOST_URL }}"
  - name: SONARQUBE_PROJECT_KEY
    value: "{{ $.Values.app.name }}"
  - name: tlsVerify
    value: "true"
  - name: IMAGE_REPO
    value: "{{ $.Values.app.IMAGE_REPO }}"

  # - name: manifestRepo
  #   value: "{{ $.Values.git.manifestRepo }}"
  # - name: manifestFile
  #   value: "{{ $.Values.git.manifestFile }}"
  # - name: manifestFileProd
  #   value: "{{ $.Values.git.manifestFileProd }}"
  # - name: manifestRepoName
  #   value: "{{ $.Values.git.manifestRepoName }}"
  # - name: cyclonedxHostUrl
  #   value: "{{ $.Values.app.cyclonedxHostUrl }}"
